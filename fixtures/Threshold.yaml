App\Entity\Threshold:
  threshold_{1..30}:
    minimum: 20
    maximum: 60
    startDate: <dateTimeBetween('-1 years', 'now')>
    endDate: <dateTimeBetween('now', '+1 years')>
    hotel: '@hotel_<current()>'